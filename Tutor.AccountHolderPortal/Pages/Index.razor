@page "/"
@namespace Tutor.AccountHolderPortal.Pages

@inject NavigationManager Nav

<PageTitle>Tutor — Account Holder Portal</PageTitle>

@code {
    protected override Task OnInitializedAsync()
    {
        Nav.NavigateTo("/statement", replace: true);
        return Task.CompletedTask;
    }
}
